# ==========================================================
# 🚦 ECOMMIND AGENCY — REDIRECTIONS COMPLÈTES
# Pipeline IA : STT → LLM → TTS → LANG → HEALTH
# ==========================================================

# === API IA ===

# 🎯 LLM : génération de texte via GPT
[[redirects]]
  from = "/api/llm"
  to = "/.netlify/functions/llm"
  status = 200
  force = true

# 🧠 STT : transcription vocale (Whisper)
[[redirects]]
  from = "/api/stt"
  to = "/.netlify/functions/stt"
  status = 200
  force = true

# 🔊 TTS : synthèse vocale (ElevenLabs)
[[redirects]]
  from = "/api/tts"
  to = "/.netlify/functions/tts"
  status = 200
  force = true

# 🌍 LANG : détection automatique de langue
[[redirects]]
  from = "/api/lang"
  to = "/.netlify/functions/lang"
  status = 200
  force = true

# 💡 HEALTH : test global des endpoints & clés
[[redirects]]
  from = "/api/health"
  to = "/.netlify/functions/health"
  status = 200
  force = true


# === FRONTEND ===

# 🧭 Redirige toutes les autres routes vers l'app SPA (index.html)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
  force = true
